- content_for :title, "投稿 - #{@thread.title} - coconikki"
- content_for :main_width, 'max-w-7xl'

/ このファイルは表示されません（newアクションは常にeditにリダイレクト）
/ 万が一表示された場合のためにリダイレクト先へのリンクを表示
.max-w-2xl.mx-auto.mt-8.text-center
  p.text-gray-600.mb-4 投稿画面に移動しています...
  = link_to "投稿画面へ", edit_thread_post_path(@thread.slug, @post), class: "text-blue-600 hover:text-blue-800 underline"
