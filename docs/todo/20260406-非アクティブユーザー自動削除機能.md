# 非アクティブユーザー自動削除機能（フェーズ2）

作成日: 2026-04-06

## 概要

完全オープンログイン化に伴い、非アクティブユーザーを自動削除する仕組みを導入する。
ユーザーの活動状況に応じて削除期間を調整し、適切なタイミングで削除を実施する。

---

## 削除ポリシー

### パターンA: 何もしてないユーザー（厳しめ）

**対象**:
- 登録後、投稿・フォロー・交換日記参加が一切ないユーザー

**除外対象**:
- 管理者（`admin?` が `true`）
- サンプル交換日記のメンバー（`is_sample: true` の交換日記に参加しているユーザー）

**削除期間**:
- **90日間**

**理由**:
- お試しだけして放置されたアカウント
- サーバーリソースの無駄遣いを防ぐ

**通知タイミング**:
- 75日目: 削除予告メール送信
- 90日目: アカウント削除実行

---

### パターンB: 活動歴があるユーザー（緩め）

**対象**:
- 投稿・フォロー・交換日記参加のいずれかがあるユーザー

**除外対象**:
- 管理者（`admin?` が `true`）
- サンプル交換日記のメンバー（`is_sample: true` の交換日記に参加しているユーザー）

**削除期間**:
- **1年間ログインなし**

**理由**:
- 一度は使ってくれた人だから長めに猶予を与える
- 再訪の可能性を考慮

**通知タイミング**:
- 345日目: 削除予告メール送信
- 365日目: アカウント削除実行

---

## 実装方針

### 1. データベース変更

**User テーブルに追加するカラム**:
```ruby
add_column :users, :last_sign_in_at, :datetime   # 最終ログイン日時
add_column :users, :last_activity_at, :datetime  # 最終アクセス日時
```

**用途**:
- `last_sign_in_at`: ログイン日時を記録（明示的なログイン操作）
- `last_activity_at`: 最終アクセス日時を記録（実際の利用状況）
- 非アクティブ判定の基準として `last_activity_at` を使用

**実装状況**:
- ✅ 2026年4月5日にマイグレーション作成済み
- ✅ SessionsController と UsernamesController で `last_sign_in_at` を記録
- ✅ ApplicationController で `last_activity_at` を5分間隔で更新

---

### 2. モデルの実装

**User モデルに追加するメソッド・スコープ**:

```ruby
# サンプル交換日記のメンバーかどうか
def sample_thread_member?
  memberships.joins(:thread).exists?(threads: { is_sample: true })
end

# 削除対象から除外するユーザー（管理者 + サンプル交換日記メンバー）
scope :deletable_users, -> {
  where.not(email: ADMIN_EMAIL_SET.to_a)  # 管理者を除外
    .where.not(id: joins(:memberships)
                    .joins("INNER JOIN threads ON threads.id = memberships.thread_id")
                    .where(threads: { is_sample: true })
                    .select(:id))  # サンプル交換日記メンバーを除外
}

# 何もしてない非アクティブユーザー（90日間）
scope :inactive_new_users, -> {
  deletable_users
    .where('last_activity_at < ? OR last_activity_at IS NULL', 90.days.ago)
    .left_joins(:posts, :subscriptions, :memberships)
    .where(posts: { id: nil }, subscriptions: { id: nil }, memberships: { id: nil })
}

# 活動歴があるが長期間ログインなし（1年間）
scope :inactive_active_users, -> {
  deletable_users
    .where('last_activity_at < ? OR last_activity_at IS NULL', 1.year.ago)
    .where.not(id: inactive_new_users.select(:id))
}

# 削除予告対象（パターンA: 75日目）
scope :warning_new_users, -> {
  deletable_users
    .where('last_activity_at BETWEEN ? AND ?', 76.days.ago, 75.days.ago)
    .left_joins(:posts, :subscriptions, :memberships)
    .where(posts: { id: nil }, subscriptions: { id: nil }, memberships: { id: nil })
}

# 削除予告対象（パターンB: 345日目）
scope :warning_active_users, -> {
  deletable_users
    .where('last_activity_at BETWEEN ? AND ?', 346.days.ago, 345.days.ago)
    .where.not(id: inactive_new_users.select(:id))
}
```

---

### 3. Rake タスクの作成

**lib/tasks/user_cleanup.rake**:

```ruby
namespace :users do
  desc "非アクティブユーザーに削除予告メールを送信"
  task send_deletion_warnings: :environment do
    # パターンA: 何もしてないユーザー（75日目）
    User.warning_new_users.find_each do |user|
      UserMailer.deletion_warning_new_user(user).deliver_later
      Rails.logger.info "Deletion warning sent to new user: #{user.email}"
    end

    # パターンB: 活動歴があるユーザー（345日目）
    User.warning_active_users.find_each do |user|
      UserMailer.deletion_warning_active_user(user).deliver_later
      Rails.logger.info "Deletion warning sent to active user: #{user.email}"
    end
  end

  desc "非アクティブユーザーを削除"
  task cleanup_inactive: :environment do
    # パターンA: 何もしてないユーザー（90日）
    new_users = User.inactive_new_users
    new_count = new_users.count
    new_users.find_each do |user|
      user.update!(deleted_at: Time.current) # 論理削除
      Rails.logger.info "Deleted inactive new user: #{user.email}"
    end

    # パターンB: 活動歴があるユーザー（1年）
    active_users = User.inactive_active_users
    active_count = active_users.count
    active_users.find_each do |user|
      user.update!(deleted_at: Time.current) # 論理削除
      Rails.logger.info "Deleted inactive active user: #{user.email}"
    end

    puts "#{new_count}人の新規非アクティブユーザーと#{active_count}人の活動歴ありユーザーを削除しました"
  end
end
```

---

### 4. SessionsController の変更

**ログイン時に last_sign_in_at を更新**:

```ruby
def create
  # ... 認証処理 ...

  user.update_column(:last_sign_in_at, Time.current)
  session[:user_id] = user.id

  # ... 以下略 ...
end
```

**実装状況**:
- ✅ 2026年4月5日に実装済み
- SessionsController の `create` アクション
- UsernamesController の `create` アクション（新規登録時）

---

### 5. メーラーの実装

**UserMailer の追加メソッド**:

```ruby
class UserMailer < ApplicationMailer
  def deletion_warning_new_user(user)
    @user = user
    @days_remaining = 15
    mail(
      to: user.email,
      subject: "【coconikki】アカウント削除予告のお知らせ"
    )
  end

  def deletion_warning_active_user(user)
    @user = user
    @days_remaining = 20
    mail(
      to: user.email,
      subject: "【coconikki】長期未ログインによるアカウント削除予告"
    )
  end
end
```

---

### 6. cron 設定

**毎日実行**:

```bash
# 毎日午前3時に削除予告メール送信
0 3 * * * cd /path/to/app && bin/rails users:send_deletion_warnings

# 毎日午前4時に非アクティブユーザー削除
0 4 * * * cd /path/to/app && bin/rails users:cleanup_inactive
```

---

## 利用規約への追記内容

```markdown
## アカウントの削除について

以下の場合、予告なくアカウントを削除する場合があります：

### 1. 登録後90日間、活動がない場合
- 投稿、交換日記への参加、フォローのいずれも行っていない場合
- 削除の15日前（登録後75日目）に通知メールをお送りします

### 2. 1年間ログインがない場合
- 過去に投稿や交換日記への参加実績があるユーザーが対象
- 削除の20日前（最終ログインから345日目）に通知メールをお送りします

### 3. その他
- スパム行為や不正利用が確認された場合
- 利用規約に違反する行為が確認された場合

## データの保持期間

削除されたアカウントのデータは、プライバシー保護のため速やかに削除されます。
```

---

## プライバシーポリシーへの追記内容

```markdown
## 個人情報の保存期間

### アクティブユーザー
- アカウントが有効な間、個人情報を保存します

### 非アクティブユーザー
- 登録後90日間活動がない場合、通知の上でアカウントを削除します
- 1年間ログインがない場合、通知の上でアカウントを削除します

### 削除後
- アカウント削除後、個人情報は速やかに削除されます
- ただし、法令で保存が義務付けられている情報は除きます
```

---

## テスト項目

- [x] `last_sign_in_at` が正しく更新されるか（実装済み）
- [x] `last_activity_at` が正しく更新されるか（実装済み）
- [ ] `sample_thread_member?` メソッドが正しく動作するか
- [ ] `deletable_users` スコープが管理者とサンプルメンバーを除外するか
- [ ] `inactive_new_users` スコープが正しく動作するか
- [ ] `inactive_active_users` スコープが正しく動作するか
- [ ] サンプル交換日記のメンバーが削除対象外になるか
- [ ] 削除予告メールが正しく送信されるか
- [ ] 削除処理が正しく実行されるか
- [ ] 削除後もデータ整合性が保たれるか

---

## 注意事項

- 論理削除（`deleted_at`）を使用するため、物理削除は行わない
- 削除予告メールは必ず送信すること（ユーザー体験の向上）
- cron の実行時間帯はサーバー負荷を考慮すること
- 本番環境での初回実行前に、ステージング環境でテストすること
- **サンプル交換日記のメンバーは削除対象外**とする（`is_sample: true` の交換日記に参加しているユーザー）
- 管理者アカウントは削除対象外とする

---

## 参考

- 関連ブランチ: feature/open-signup-with-quota
- 実装担当: （後日決定）
- 予定日: 運用状況を見ながら判断

## 関連ファイル

- `app/models/user.rb` - User モデル
- `app/models/correspondence_thread.rb` - CorrespondenceThread モデル（`is_sample` カラム使用）
- `app/controllers/application_controller.rb` - `track_user_activity` メソッド
- `app/controllers/sessions_controller.rb` - `last_sign_in_at` 更新処理
- `app/controllers/usernames_controller.rb` - `last_sign_in_at` 更新処理
- `app/views/pages/privacy.html.slim` - プライバシーポリシー（自動削除の予告記載済み）
- `db/schema.rb` - `users.last_activity_at`, `users.last_sign_in_at`, `threads.is_sample` カラム

## 既存の関連実装

- ✅ 2026年4月5日: `last_sign_in_at` と `last_activity_at` カラム追加
- ✅ 2026年4月5日: アクティビティトラッキング機能実装
- ✅ 2026年4月6日: プライバシーポリシーに自動削除ポリシー記載
- ✅ CorrespondenceThread モデルに `is_sample` カラム存在
  - `scope :sample_threads, -> { where(is_sample: true) }`
  - `scope :user_threads, -> { where(is_sample: false) }`
